const snacks = ["Chocolate", "Snowbunnies", "Gummybears", "Boba", "Chips", "Cookies"]

function printSnacks(snacksL: string[]) {
    for (let i = 0; i < snacksL.length; i++) {
        console.log(snacksL[i]);
    }
}

printSnacks(snacks);
